import { FiltroPerfilesPipe } from './filtro-perfiles.pipe';

describe('FiltroPerfilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPerfilesPipe();
    expect(pipe).toBeTruthy();
  });
});
